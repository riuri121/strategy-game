# Tests

Test suites for both Python and Rust components.

## Python Tests

Run tests from the project root:

```bash
pytest
```

## Rust Tests

Run tests from the project root:

```bash
cargo test
```
